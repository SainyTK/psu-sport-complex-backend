const localhost = {
    host: 'localhost',
    username: '',
    password: '',
    database: '',
};

const remoteHost = {
    host: '',
    username: '',
    password: '',
    database: '',
};

export const dbConfig = localhost;
